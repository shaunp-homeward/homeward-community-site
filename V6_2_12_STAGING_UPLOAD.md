# V6.2.12 Staging Upload

Apply the targeted update to the current V6.2.11 staging branch, preserving the folder structure. Netlify should continue using the existing `npm run build` command.

## Test after deploy
1. `/circles` — confirm the new hero lead, Circle rhythm, sample gathering, and eight-week commitment statement.
2. `/about` — confirm the primary community button reads “Have a Conversation.”
3. Check the nav, mobile menu, footer, and interest prompt on several pages. All calendar-opening call buttons should read “Have a Conversation.”
4. Open the Circle sample and confirm italicized words render as italics rather than visible HTML tags.
5. Confirm no color, font, spacing, or layout changes.
